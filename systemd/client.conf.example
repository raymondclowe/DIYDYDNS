# DIY Dynamic DNS Client Configuration
# This file should be placed at /etc/diydydns/client.conf

# Server connection (user@hostname or user@ip)
SERVER=user@your-server.com

# Path on the server where the IP file will be stored
REMOTE_PATH=/var/www/html/myip.txt

# Check interval in seconds (default: 300 = 5 minutes)
INTERVAL=300

# Optional: SSH key for authentication (leave empty to use default)
# SSH_KEY=/home/username/.ssh/id_rsa
